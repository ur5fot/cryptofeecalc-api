# CryptoFeeCalc API

Minimal API scaffold for fee estimation.

## Setup

```bash

npm install
npm run dev
```

## Endpoints

- `GET /health` -> `{ "status": "ok" }`
- `POST /api/estimate` -> stub response (not implemented)
